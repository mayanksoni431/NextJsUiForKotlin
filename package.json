{
  "name": "mynextapp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
   "dev": "npm ci && next build && next dev -p 8080",
    "build": "next build",
    "start": "npm ci && next build && next start -p 8080",
    "lint": "next lint"
  },
  "dependencies": {
    "react": "^18",
    "react-dom": "^18",
    "next": "13.5.4"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10",
    "postcss": "^8",
    "tailwindcss": "^3",
    "eslint": "^8",
    "eslint-config-next": "13.5.4"
  }
}
